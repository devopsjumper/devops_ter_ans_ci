#these would be route53 names generated by terraform
#another way is to use ansible to parse the terraofmr provisioner file which
#has all the facts

[webservers]
10.0.1.0/24
10.0.3.0/24

[database]
10.0.2.0/24
10.0.4.0/24
